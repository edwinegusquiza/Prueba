git config --global user.name "Edwin Egusquiza"
git config --global user.email edwinegusquiza@gmail.com

git config --list
git config user.name

git init

git add archivo.txt
git status
git add --all

git commit -m "comentario"

git log

git remote add origin https://github.com/edwinegusquiza/Prueba.git
git remote -v (verificar conexion remota)

git push origin master

